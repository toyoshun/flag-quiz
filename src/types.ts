export type Country = {
  name: string;
  code: string;
  region: string;
};

export type Mode = "easy" | "hard";

export type Region = "World" | "Africa" | "Americas" | "Asia" | "Europe" | "Oceania";
